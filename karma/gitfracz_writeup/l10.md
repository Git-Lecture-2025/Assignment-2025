# Git Exercises - case-sensitive-filename 
```
git start case-sensitive-filename
```
**Level Description ->** change the file name (simple)
```
git mv File.txt file.txt
git commit -sS -am "rename file"
git verify
```
