# Git Exercises - chase-branch 
```
git start chase-branch
```
**Level Description ->** Merge the chase-branch with escaped branch.
```
git checkout chase-branch #brings to the required branch if not there already.
git merge escaped
git verify
```
