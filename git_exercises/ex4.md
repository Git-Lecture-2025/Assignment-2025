1. Create a `.gitignore` file using `touch` command and write the following in it using any text editor.
```
*.exe
*.o
*.jar
libraries/
```
2. then commit the changes
```
git add .gitignore
git commit -m "added .gitignore"
```